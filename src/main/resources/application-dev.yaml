eureka:
  instance:
    preferIpAddress: true
    hostname: eureka-0
  client:
    service-url:
      defaultZone: http://eureka-service:8761/eureka
  

spring:
  data:
    mongodb:
      uri:  # we replace by real db url
      host: atlas-cluster.mqdj7js.mongodb.net
      port: 27017
      database: orderdb
      repositories:
        enabled: true
